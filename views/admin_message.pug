doctype html
head
    meta(charset='UTF-8')
di.contact
    include ./partial/msg
    h3 Messages Admin Panel
    - for(let i = 0; i < message.length; i++){
        li= message[i].custom_id
        li= message[i].email_address
        li= message[i].title
        li= message[i].message
        li= message[i].time
        br
    -}



